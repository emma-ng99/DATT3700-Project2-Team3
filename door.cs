using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class door : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        keys key = GetComponent<keys>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
